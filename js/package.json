{
  "name": "versioner-js-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "pnpm workspace for the JavaScript packages in the versioner repo: @officespacesoftware/versioner and @officespacesoftware/release-management-{core,mcp,cli}",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r build",
    "clean": "pnpm -r clean",
    "dev": "pnpm -r --parallel dev",
    "release": "node scripts/release.mjs"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.1",
    "typescript": "^5.7.2"
  }
}
